# Leave empty in local dev so the browser calls same-origin /api (Nuxt proxies to Nest).
# Set to the absolute API URL only when the web app does not proxy /api (e.g. production).
NUXT_PUBLIC_API_URL=
NUXT_PUBLIC_APP_URL=http://localhost:3000
NUXT_PUBLIC_API_PROXY_TARGET=http://localhost:3001
